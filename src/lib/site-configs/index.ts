/**
 * Public re-export of the default site configuration. Forkers should edit
 * `src/lib/site.config.ts` directly to rebrand the template — no preset
 * switcher is shipped in this build.
 */

export { DEFAULT_SITE_CONFIG } from '../site.config';
export type { SiteConfig } from '../site.config';